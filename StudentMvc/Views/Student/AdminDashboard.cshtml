@{
    ViewBag.Title = "Admin Dashboard";
}

<style>
    .admin-dashboard {
        max-width: 600px;
        margin: 60px auto;
        padding: 40px;
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        text-align: center;
        font-family: 'Segoe UI', sans-serif;
    }

        .admin-dashboard h2 {
            color: #2c3e50;
            font-size: 28px;
            margin-bottom: 10px;
        }

        .admin-dashboard p {
            font-size: 18px;
            color: #555;
        }
</style>

<div class="admin-dashboard">
    <h2>Welcome, Admin!</h2>
    <p>Use the top navigation bar to manage students and marks.</p>
</div>
